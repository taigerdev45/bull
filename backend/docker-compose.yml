version: '3.8'

services:
  backend:
    build: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DJANGO_DEBUG=True
    env_file:
      - .env
    command: python manage.py runserver 0.0.0.0:8000
